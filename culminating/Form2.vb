Public Class tempForm

    Private Sub Button1_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button1.Click
        Dim sinCelc As Single = Single.Parse(TextBox1)
        Dim sinFer As Single = Single.Parse(TextBox2)
        Dim sinKel As Single = Single.Parse(TextBox3)

        If (sinFer = "" And sinKel = "" And sinCelc) Then

        End If
    End Sub
End Class